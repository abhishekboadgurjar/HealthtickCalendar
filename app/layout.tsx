import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "HealthTick Calendar - Coaching Call Management",
  description:
    "Professional calendar system for managing coaching calls with smart scheduling and recurring appointments.",
  keywords: "calendar, coaching, appointments, scheduling, healthcare",
    generator: 'abhishek'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
